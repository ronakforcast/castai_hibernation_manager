apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cast-hibernate-manager.fullname" . }}-test-connection"
  labels:
    {{- include "cast-hibernate-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cast-hibernate-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
