apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-hibernate-manager-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-hibernate-manager-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-hibernate-manager-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
